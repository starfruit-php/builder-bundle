
<section>
    {{ include(builderEditmodeTitle) }}
    {{ pimcore_link(name, {
        disabledFields: 'target, parameters, anchor, title, accesskey, tabindex, class, attributes'
    }) }}
</section>